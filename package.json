{
  "name": "tidegate",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check-types": "bun run --filter '*' check-types",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
